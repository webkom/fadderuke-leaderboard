version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  spreadsheet-api:
    lang: golang-http
    handler: ./spreadsheet-api
    image: spreadsheet-api:latest
    secrets:
      - google-sheets-credentials
    build_args:
      GO111MODULE: on

